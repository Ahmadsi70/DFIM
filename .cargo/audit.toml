[advisories]
ignore = [
    # RUSTSEC-2023-0071: rsa — Marvin Attack (timing sidechannel, medium 5.9)
    # No fix available. Only reachable via sqlx-mysql's caching_sha2_password auth,
    # which is NOT used — DFIM uses PostgreSQL exclusively. The code path is never
    # exercised in our deployment.
    "RUSTSEC-2023-0071",
    # RUSTSEC-2025-0057: fxhash — unmaintained transitive dep
    "RUSTSEC-2025-0057",
    # RUSTSEC-2026-0173: proc-macro-error2 — unmaintained transitive dep
    "RUSTSEC-2026-0173",
]